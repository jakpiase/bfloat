name: Build

on: push

jobs:
  build_wheels_36:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-18.04, macos-10.15, windows-2019]
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.6"

      - name: Setup pip
        run: |
          python -m pip install --upgrade pip
          python -m pip uninstall --yes numpy
          python -m pip uninstall --yes paddle_bfloat
          python -m pip cache purge
          python -m pip install --force-reinstall --no-cache-dir numpy==1.13
          python -m pip install cibuildwheel==2.4.0

      - name: Clone submodules
        run: git submodule update --init --recursive

      - name: Build wheel
        run: python -m cibuildwheel --output-dir dist/
        env:
          CIBW_BUILD: cp36-*

      - uses: actions/upload-artifact@v2
        with:
          path: ./dist/*.whl
          
  build_wheels_37:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-18.04, macos-10.15, windows-2019]
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.7"

      - name: Setup pip
        run: |
          python -m pip install --upgrade pip
          python -m pip install cibuildwheel==2.4.0

      - name: Clone submodules
        run: git submodule update --init --recursive

      - name: Build wheel
        run: python -m cibuildwheel --output-dir dist/
        env:
          CIBW_BUILD: cp37-*
          CIBW_BEFORE_BUILD: python -m pip install --force-reinstall --no-cache-dir numpy==1.15

      - uses: actions/upload-artifact@v2
        with:
          path: ./dist/*.whl
          
  build_wheels_38:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-18.04, macos-10.15, windows-2019]
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.8"

      - name: Setup pip
        run: |
          python -m pip install --upgrade pip
          python -m pip uninstall --yes numpy
          python -m pip uninstall --yes paddle_bfloat
          python -m pip cache purge
          python -m pip install --force-reinstall --no-cache-dir numpy==1.18
          python -m pip install cibuildwheel==2.4.0

      - name: Clone submodules
        run: git submodule update --init --recursive

      - name: Build wheel
        run: python -m cibuildwheel --output-dir dist/
        env:
          CIBW_BUILD: cp38-*

      - uses: actions/upload-artifact@v2
        with:
          path: ./dist/*.whl
          
  build_wheels_39:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-18.04, macos-10.15, windows-2019]
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.9"

      - name: Setup pip
        run: |
          python -m pip install --upgrade pip
          python -m pip uninstall --yes numpy
          python -m pip uninstall --yes paddle_bfloat
          python -m pip cache purge
          python -m pip install --force-reinstall --no-cache-dir numpy==1.20
          python -m pip install cibuildwheel==2.4.0

      - name: Clone submodules
        run: git submodule update --init --recursive

      - name: Build wheel
        run: python -m cibuildwheel --output-dir dist/
        env:
          CIBW_BUILD: cp39-*

      - uses: actions/upload-artifact@v2
        with:
          path: ./dist/*.whl
          
  build_wheels_310:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-18.04, macos-10.15, windows-2019]
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.10"

      - name: Setup pip
        run: |
          python -m pip install --upgrade pip
          python -m pip uninstall --yes numpy
          python -m pip uninstall --yes paddle_bfloat
          python -m pip cache purge
          python -m pip install --force-reinstall --no-cache-dir numpy==1.22
          python -m pip install cibuildwheel==2.4.0

      - name: Clone submodules
        run: git submodule update --init --recursive

      - name: Build wheel
        run: python -m cibuildwheel --output-dir dist/
        env:
          CIBW_BUILD: cp310-*

      - uses: actions/upload-artifact@v2
        with:
          path: ./dist/*.whl
